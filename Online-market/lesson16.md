# Урок 16: CSS Grid — основи та створення сіток

## Мета уроку
На цьому уроці ви навчитесь:
- створювати сітки для сторінок магазину
- розміщувати картки товарів у рядках і колонках
- комбінувати Grid з Flexbox для гнучких макетів

Працюємо у файлі:
css/style.css

## Крок 1. Що таке CSS Grid

Grid — це система розташування елементів
у **рядках та колонках**.

Зручно для:
- каталогів товарів
- головних сторінок
- блоків з різною висотою карток

## Крок 2. Grid-контейнер
Щоб побачити як працює Grid додайти в секцію ``new-arrivalse`` теж продукти, але змініть назву класу контейнера на ``products-new`` 

```html
<section id="new-arrivals">
      <h2>Новинки</h2>
      <!-- Тут будуть нові товари -->
    <div class="products-new">
      <article class="product-card">
        <img src="./images//sale.png" alt="badge" class="badge">
        <h3>Рюкзак X123</h3>
        <!-- Картинка товару 1 -->
        <img src="./images/1.png" alt="Світильник">
        <!-- Посилання на сторінку товару 1 -->
        <a href="./products/tovar1.html">Детальніше...</a>
        <p>Рукзак: X123 у чорному кольорі</p>
      </article>
      <article class="product-card">
        <h3>Смарт-Годинник Y456</h3>
        <!-- Картинка товару 2 -->
        <img src="./images/2.png" alt="Товар 2">
        <!-- Посилання на сторінку товару 2 -->
        <a href="./products/tovar2.html">Детальніше...</a>
        <p>Смарт-Годинник Y456 </p>
      </article>
    </div>
    </section>

    ```

Щоб Grid працював, потрібно задати контейнеру:

```css
.products-new {
  display: grid;
}
```
Усі вкладені елементи стають Grid-елементами

## Крок 3. Колонки та рядки

Задаємо кількість колонок:

```css
.products-new {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
}
```
- 3 колонки однакової ширини
- елементи автоматично заповнюють рядки

## Крок 4. Відступи між елементами

Використовуємо `gap` для відстані:

```css
.products-new {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
}
```
💡 Тепер картки не «злипаються»
## Крок 5. Рядки автоматичної висоти

Grid автоматично підлаштовує висоту рядків
під вміст карток.

```css
.products-new {
  grid-auto-rows: minmax(150px, auto);
}
```


💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson16"
git push  опиши цей крок для першого уроку

```

## Крок 7. 📝 Практичне завдання для магазину

1. Створити каталог з 6 карток товарів
2. Використати Grid для розташування у 3 колонки
3. Зробити відступи між картками
4. Внутрішні елементи картки — Flexbox

🎯 Головна сторінка магазину стає структурованою

## Крок 8. Типові помилки

- не задавати display: grid
- плутати колонки і рядки
- забувати про відступи (gap)

❗ Grid простий, якщо дотримуватись базових правил




