# Урок 14: Flexbox — основи та практичні приклади

## Мета уроку
На цьому уроці ви навчитесь:
- розташовувати елементи в ряд або колонку
- рівномірно вирівнювати блоки
- створювати базову сітку товарів для магазину

Працюємо у файлі:
css/style.css

## Крок 1. Що таке Flexbox

Flexbox — це спосіб розміщення елементів
в одному напрямку (ряд або колонка).

Flexbox зручно використовувати для:
- карток товарів
- меню
- кнопок
- простих макетів

## Крок 2. Flex-контейнер

1. Обгорніть карточки продуктів на головній сторінці у `div` з  класом `products`.
2. Щоб Flexbox запрацював, потрібно задати контейнеру:

```css
.products {
  display: flex;
}
```
Після цього всі вкладені елементи стають флекс-елементами

## Крок 3. Напрямок елементів

Властивість `flex-direction` визначає напрямок:

- row — в ряд (за замовчуванням)
- column — в колонку

Приклад:
```css
.products {
  display: flex;
  flex-direction: row;
  width: 100%;
}  display: flex;
  flex-direction: column;
  justify-content: space-between;
```
💡 Для карток товарів зазвичай використовується row

## Крок 4. Вирівнювання по головній осі

Властивість `justify-content` керує вирівнюванням по горизонталі.

Основні значення:
- flex-start
- center
- space-between
- space-around

Приклад:

```css
.featured-products {
  justify-content: space-between; 
}

```
💡 Спробуйте різні властивості, щоб зрозуміти різницю між ними, для цього додайте ще більше карточок товару, задайте їм розміри



## Крок 5. Вирівнювання по вертикалі

Властивість `align-items` вирівнює елементи по вертикалі.

Приклад:
```css
.featured-products {
  align-items: center;
}
```
💡 Корисно для кнопок та карток однакової висоти

## 📝 Завдання: 
> Додайте flex-властивості в меню в `<header>` з вирівнюванням `justify-content: space-between`

## Крок 6. Перенос елементів

Якщо елементів багато, вони можуть не вміститись в один ряд.

Для цього використовують:
```css
.featured-products {
  flex-wrap: wrap;
}
```
# Крок 7. Практика для магазину

На цьому етапі:
- контейнер товарів — flex
- картки товарів — рівномірно розташовані
- елементи переносяться на новий ряд

🎯 Це перша справжня сітка товарів

💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson14"
git push  опиши цей крок для першого уроку

```

## Крок 8. Типові помилки

- забути display: flex
- плутати justify-content та align-items
- не використовувати flex-wrap

❗ Flexbox простий, але потребує уважності

## Підсумок уроку

На цьому уроці ви:
- освоїли Flexbox
- навчилися вирівнювати елементи
- створили базову сітку товарів

У наступному уроці:
Flexbox — складні схеми та відступи
