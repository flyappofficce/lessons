# Урок 8: CSS-класи

## Мета уроку
На цьому уроці ви навчитесь:
- що таке CSS-клас
- навіщо потрібні класи
- як оформлювати окремі частини сторінки

Ми почнемо стилізувати блоки нашого онлайн-магазину:
- шапку
- основний контент

## Крок 1. Що таке CSS-клас

CSS-клас — це ім’я, яке ми даємо елементу,
щоб оформити його окремо від інших.

Клас:
- починається з крапки `.` у CSS
- додається в HTML через `class`

Приклад CSS:
```css
.title {
  color: blue;
}
```
Приклад HTML:
```html
<h1 class="title">Назва магазину</h1>
```
## Крок 2. Додаємо клас до заголовка магазину

1. Відкрийте файл `index.html`
2. Знайдіть головний заголовок магазину (назву сайту)
3. Додайте йому клас `shop-title`

❗ Не переписуйте весь код.
Потрібно лише додати `class=""` до вже існуючого тегу.

## Крок 3. Стилізуємо назву магазину

1. Відкрийте файл `css/style.css`
2. Додайте стиль для класу `.shop-title`

Приклад:
```css
.shop-title {
  font-size: 28px;
  text-align: center;
}
```
💡 Пояснення:
- font-size — розмір тексту
- text-align: center — вирівнювання по центру

## Крок 4. Створюємо контейнер для контенту

Магазин має бути структурованим.
Для цього ми використовуємо контейнер.

1. У `index.html` знайдіть основний контент сторінки
2. Обгорніть його в елемент з класом `container`

❗ Не додавайте нові теги самі.
Працюємо тільки з тим, що вже є.

## Крок 5. Оформлення контейнера

У файлі `style.css` додайте:

```css
.container {
  max-width: 1000px;
  margin: 0 auto;
}
```
💡 Пояснення:
- max-width — максимальна ширина блоку
- margin: 0 auto — центрує контейнер по горизонталі
Тепер магазин виглядає як окрема сторінка, а не «розтягнутий» сайт.

## Крок 6. Оформлення текстових блоків

Якщо у вас є абзаци з описом магазину:

1. Додайте їм клас `shop-description`
2. У `style.css` створіть стиль для цього класу

Приклад:
```css
.shop-description {
  font-size: 16px;
  line-height: 1.5;
}
```
💡 line-height робить текст легшим для читання
## Крок 7. Перевірка

1. Збережіть `style.css`
2. Відкрийте `index.html` у браузері
3. Перевірте:
   - назва магазину по центру
   - контент не прилипає до країв
   - текст легко читається

  ## Крок 8. Стилізація інших сторінок
  На цьому етапі потрібно застосувати ті самі стилі, які ви вже використали на головній сторінці, до інших сторінок сайту.
  >⚠️ Не забудьте додати класи до елементів на всіх сторінках сайту

 **Що потрібно зробити:**
* Переконайтесь, що файл style.css підключений на всіх HTML-сторінках.
* Використовуйте однакові класи для однакових елементів (навігація, заголовки, контент).
* Перевірте, щоб сторінки виглядали в одному стилі та не “ламалися” при переходах.

🎯 Мета кроку:
Зробити всі сторінки онлайн-магазину візуально цілісними та акуратними.


   💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson8"
git push  опиши цей крок для першого уроку

```

   ## Крок 8. Підсумок уроку

На цьому уроці ви:
- дізнались, що таке CSS-клас
- навчилися додавати класи в HTML
- стилізували окремі частини магазину
- зробили структуру сторінки охайною

🎯 Магазин починає виглядати як справжній сайт.

У наступному уроці ми:
- навчимось працювати з селекторами
- оформимо декілька елементів одразу
