# Урок 4: Форми та input-и

## Мета уроку
У цьому уроці ви навчитеся:
- Створювати форму замовлення товару.
- Використовувати різні типи полів `<input>`.
- Додавати кнопки для відправки форми.

Цей урок продовжує роботу над вашим онлайн-магазином. У вас вже є сторінки з категоріями та характеристиками товарів.

---

## Крок 1. Створення сторінки з формою

1. У папці `products` створіть файл `order_form.html`.
2. Додайте заголовок сторінки:

```html
<h1>Оформлення замовлення</h1>
```
3. Пояснення:

Ми будемо робити основну форму замовлення на одному файлі.
Тег `<form>` використовується для збору даних від користувача.

## Крок 2. Додавання полів форми

Усередині `<form>` додаємо різні поля:

```html
<form>
  <!-- Ім'я покупця -->
  <label for="name">Ім'я:</label>
  <input type="text" id="name" name="name" placeholder="Введіть ваше ім'я">

  <!-- Електронна пошта -->
  <label for="email">Email:</label>
  <input type="email" id="email" name="email" placeholder="example@mail.com">

  <!-- Телефон -->
  <label for="phone">Телефон:</label>
  <input type="tel" id="phone" name="phone" placeholder="+380123456789">
</form>
```
 💡 Порада:
- Кожне поле має id та name, щоб дані можна було обробити пізніше.
- Ми ще не підключаємо JS для відправки, просто створюємо форму.

## Крок 3. Додавання кнопки відправки

Усередині `<form>` перед закриваючим еугом  `</form>` додаємо кнопку:

```html
<button type="submit">Оформити замовлення</button>
```
2. Пояснення:
- type="submit" відправляє форму.
- Поки JS не підключено, кнопка просто перезавантажить сторінку.
- Згодом ми зробимо, щоб форма працювала з перевіркою і зберіганням даних.

## Крок 4. Додавання посилання на форму в меню

1. Відкрийте index.html.
2. Додайте новий пункт меню:

```html
<nav>
  <a href="./products/categories.html">Категорії товарів</a>
  <a href="./products/purchase_steps.html">Як купити</a>
  <a href="./products/product_specs.html">Характеристики</a>
  <a href="./products/order_form.html">Замовлення</a>
</nav>
```

* Додайте меню на всі сторінки після тегу `<body`, це дозволить користувачу переходити до форми замовлення з будь-якої сторінки, враховуючи використання різних символів (``/`, `./`, `../`, ) у посиланнях, в залежності від їх розміщення у папках  
* Додайте посилання на головну сторінку index.html

```html

<a href="index.html">Головна</a>

```

💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson4"
git push  опиши цей крок для першого уроку

```

## Крок 5. Перевірка форми

1. Відкрийте `order_form.html` у браузері.
2. Перевірте, що:
   - Поля для введення ім'я, email, телефон з’являються.
   - Кнопка “Оформити замовлення” є внизу.
   - Посилання в меню веде на цю сторінку.

💡 Порада:
- Навіть якщо форма поки не зберігає дані, важливо перевіряти **структуру та порядок елементів**.

## Крок 6. Підсумок уроку

- Створили **сторінку форми замовлення** `order_form.html`.
- Додали різні **поля `<input>`**: текст, email, телефон.
- Додали **кнопку submit** для відправки форми.
- Додали **посилання на форму** у меню на головній сторінці.
- Впорядкували проєкт так, щоб студент бачив **структуровану навігацію між сторінками**.

> Наступного разу ми перейдемо до **Уроку 5: Семантичні теги HTML5**, де зробимо сторінки більш структурованими та зрозумілими.


