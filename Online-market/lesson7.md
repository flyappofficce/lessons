# Урок 7: Основи CSS

## Мета уроку
У цьому уроці ви навчитесь:
- розуміти, що таке CSS-правило
- змінювати колір тексту
- змінювати розмір шрифту
- додавати відступи

Ми будемо оформлювати головну сторінку нашого онлайн-магазину.

## Крок 1. Що таке CSS і як він працює

CSS — це мова стилів.  
Вона відповідає за те, **як виглядає сайт**, а не що на ньому написано.
CSS працює за таким принципом: елемент → властивість → значення

Приклад:

```css
p {
  color: red;
}
```
💡 Пояснення:
- p — елемент (абзац)
- color — властивість
- red — значення

## Крок 2. Відкриваємо файл стилів

1. Відкрийте папку `css`
2. Відкрийте файл `style.css`

❗ Усі зміни в цьому уроці ми робимо ТІЛЬКИ в `style.css`  
HTML-файл не чіпаємо.

## Крок 3. Зміна кольору фону сайту

У файлі `style.css` знайдіть або додайте стиль для `body`.

Додайте або змініть властивість:

```css
body {
  background-color: #ffffff;
}
```
💡 Пояснення:
- body — уся сторінка
- background-color — колір фону
 - #ffffff — білий колір
👉 Можна спробувати інші кольори:
- #f5f5f5
- #eeeeee


## Крок 4. Зміна кольору тексту

Додайте в `style.css` до `body{}` стиль для тексту сторінки:

```css
body {
  color: #554f4f;
}
```
💡 Пояснення:
- color змінює колір тексту
- Темно-сірий колір легше читати, ніж чорний

## Крок 5. Зміна розміру шрифту

У `style.css` до `body{}` додайте:

```css
body {
  font-size: 16px;
}
```
💡 Пояснення:
- font-size — розмір тексту
- px — пікселі
- 16px — стандартний зручний розмір

## Крок 6. Відступи всередині сторінки

Додайте відступи для всієї сторінки:

```css
body {
  padding: 20px;
}
```
💡 Пояснення:
- padding — внутрішній відступ
- Текст не прилипає до країв екрана
- Сайт виглядає акуратніше

## Крок 7. Перевірка змін

1. Збережіть файл `style.css`
2. Відкрийте `index.html` у браузері
3. Перевірте:
   - фон сторінки
   - колір тексту
   - відступи від країв

❗ Якщо змін не видно — оновіть сторінку (F5)

## Крок 8. Підсумок уроку

На цьому уроці ви:
- попрацювали з файлом `style.css`
- змінили фон сайту
- змінили колір і розмір тексту
- додали відступи

🎯 Ваш онлайн-магазин почав виглядати охайно та читабельно.

💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson7"
git push  опиши цей крок для першого уроку

У наступному уроці ми:
- будемо оформлювати окремі блоки
- навчимось працювати з класами
