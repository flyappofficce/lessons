# Урок 6: Підключення CSS та JS

## Мета уроку
У цьому уроці ви:
- Дізнаєтесь, навіщо потрібні CSS та JavaScript
- Створите файли стилів і скриптів
- Навчитесь правильно підключати їх до HTML

Це перший крок до того, щоб ваш онлайн-магазин **виглядав красиво** і **реагував на дії користувача**.
## Крок 1. Створення папок для стилів і скриптів

1. Відкрийте папку вашого проєкту **онлайн-магазину**.
2. Усередині неї:
   - створіть папку `css`
   - створіть папку `js`

👉 Як створити папку:
- Клік правою кнопкою → **New Folder**
- Назвіть папку відповідно: `css`, `js`

📁 Структура проєкту тепер виглядає так:

project/
- index.html
- products/
- css/
- js/

## Крок 2. Створення файлу стилів (CSS)

1. Відкрийте папку `css`
2. Створіть файл **style.css**

👉 Як створити файл:
- Правий клік → **New File**
- Назвіть файл: `style.css`

3. Відкрийте `style.css` і додайте один простий стиль:

```css
body {
  background-color: #f5f5f5;
}
```
💡 Пояснення:
- CSS відповідає за зовнішній вигляд сайту
- background-color змінює колір фону сторінки

## Крок 3. Підключення CSS до сайту

1. Відкрийте файл `index.html`
2. Знайдіть тег `<head>`
3. Усередині `<head>` додайте посилання на CSS:

```html
<link rel="stylesheet" href="css/style.css">
```
💡 Пояснення:
- `<link>` використовується для підключення стилів
- href — шлях до файлу стилів
- Тепер браузер знає, де брати CSS


---

## 📄 `lesson6_step5.md`

```markdown
## Крок 4. Створення файлу JavaScript

1. Відкрийте папку `js`
2. Створіть файл **script.js**

3. Відкрийте `script.js` і додайте:

```javascript
console.log("Скрипт підключено");
```
💡 Пояснення:
- JavaScript відповідає за поведінку сайту
- console.log — це повідомлення для розробника
- Його буде видно в консолі браузера

## Крок 5. Підключення JavaScript до HTML

1. Відкрийте `index.html`
2. Знайдіть закриваючий тег `</body>`
3. **Перед ним** додайте:

```html
<script src="js/script.js"></script>
```
💡 Пояснення:
- `<script>` підключає JavaScript
- Скрипт підключається внизу, щоб сторінка встигла завантажитись

## Крок 6. Перевірка роботи CSS та JS

1. Відкрийте `index.html` у браузері
2. Перевірте:
   - Фон сторінки змінився (CSS працює)
3. Відкрийте:
   - Правий клік → **Inspect**
   - Вкладка **Console**
4. Ви маєте побачити повідомлення:
   "Скрипт підключено"

✅ Якщо бачите фон і повідомлення — все підключено правильно

## Крок 7. Підсумок уроку

- Створили папки `css` і `js`
- Створили файли `style.css` та `script.js`
- Підключили CSS через `<link>`
- Підключили JS через `<script>`
- Перевірили роботу стилів і скриптів у браузері

🎯 Тепер ваш онлайн-магазин готовий до стилізації і інтерактивності.

> У наступному уроці ми почнемо **писати CSS**: кольори, шрифти та базове оформлення магазину.
