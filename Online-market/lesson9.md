# Урок 9: CSS-селектори

## Мета уроку
На цьому уроці ви дізнаєтесь:
- що таке CSS-селектор
- які селектори ми вже використовували
- чим відрізняється селектор елемента від селектора класу

Ми навчимось стилізувати:
- всі однакові елементи одразу
- окремі елементи через клас

## Крок 1. Що таке CSS-селектор

CSS-селектор — це спосіб сказати браузеру:
«Який саме елемент потрібно оформити».

Приклад:
```css
p {
  color: black;
}
```
Цей селектор означає:
- оформити всі абзаци `<p>` на сторінці.

## Крок 2. Селектор елемента

Селектор елемента — це назва HTML-тегу.

Приклади:
- `p` — всі абзаци
- `h1` — всі заголовки h1
- `a` — всі посилання

Завдання:
1. Відкрийте `css/style.css`
2. Додайте стиль для всіх абзаців магазину

Приклад:
```css
p {
  font-size: 15px;
}
```
❗ Це вплине на всі `<p>` на сторінці

## Крок 3. Селектор класу (нагадування)

Селектор класу починається з крапки `.`

Приклад:
```css
.shop-title {
  color: darkblue;
}
```
Цей стиль застосовується тільки до елементів,
які мають class="shop-title"

💡 Клас дозволяє оформлювати окремі частини магазину

## Крок 4. Коли використовувати елемент, а коли клас

Селектор елемента:
- коли всі елементи мають виглядати однаково
- наприклад: всі абзаци

Селектор класу:
- коли потрібен особливий стиль
- наприклад: опис магазину або назва

❗ У реальних проєктах зазвичай використовують класи

## Крок 5. Оформлення посилань магазину

У нашому магазині вже є посилання:
- на сторінки товарів
- на інші розділи

Завдання:
1. У `style.css` знайдіть або додайте стиль для `a`

Приклад:
```css
a {
  text-decoration: none;
}
```
💡 text-decoration: none прибирає підкреслення

## Крок 6. Робимо посилання більш охайними

Додайте ще одну властивість до стилю `a`, колір можете замінити на інший:

```css
a {
  color: #53350e;
}
```

💡 Тепер усі посилання магазину виглядають однаково
і не відволікають увагу

## Крок 7. Перевірка

1. Збережіть `style.css`
2. Відкрийте `index.html` у браузері
3. Перевірте:
   - всі абзаци мають однаковий розмір
   - посилання без підкреслення
   - стиль застосувався автоматично

   💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson9"
git push  опиши цей крок для першого уроку

```

   ## Крок 8. Підсумок уроку

На цьому уроці ви:
- дізнались, що таке CSS-селектор
- використовували селектор елемента
- повторили селектор класу
- зробили стилі більш системними

🎯 Магазин стає чистішим і структурованішим.

У наступному уроці:
- кольори
- фони
- візуальний стиль магазину
