# Урок 13: Позиціонування елементів у CSS

## Мета уроку

На цьому уроці ви навчитесь:

- розуміти, як браузер розміщує елементи
- змінювати положення елементів на сторінці
- використовувати позиціонування для елементів магазину

Працюємо з файлом:
css/style.css

## Крок 1. Що таке position

Властивість `position` визначає,
**як елемент розташовується на сторінці**.

Основні значення:

- static
- relative
- absolute
- fixed
- sticky

## Крок 2. position: static

`static` — значення за замовчуванням.

Елемент:

- розташовується зверху вниз
- зліва направо
- не реагує на top, left, right, bottom

Приклад:

```css
.product {
  position: static;
}
```

💡 Більшість елементів на сторінці саме такі

## Крок 3. position: relative

`relative` дозволяє трохи змістити елемент від його звичайного місця.
>👉 Якщо блоку з `relative` задати зміщення, наприклад `top:5px;` , то треба розуміти, що він перекриє елемент нище нього. 


Задайте стиль карточки продуктів на головній сторінці.
Приклад:

```css
.product-card {
  position: relative;
  padding: 16px;
}
```

Елемент:

- зсувається вниз
- але його місце залишається зайнятим

## Крок 4. position: absolute

`absolute` повністю "виймає" елемент з потоку сторінки.

Приклад:

1. Знайдіть картинку sale.png в папці images цього репозиторію. 2. Додайте її в папку images свого проекту.
2. Додайте на сторінці index.html в перший блок `product-card` це зображення як `<img>`
3. Задайте зображенню клас `badge`

```css
.badge {
  position: absolute;
  top: 5px;
  left: 10px;
  width: 35px;
  height: 30px;
}
```

💡Позиціювання часто використовується для:

- бейджів "NEW"
- знижок
- іконок

## Крок 5. position: fixed

`fixed` фіксує елемент відносно вікна браузера.

Приклад:
Задайте стиль для `<header>`

```css
header {
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 1000;
  background-color: #ffffff;
  /* світлий фон для header */
  border-bottom: 1px solid #bdc3c7;
  /* легка межа */
}
```

👉 потрібно додати відступ зверху для <main> або в нас це клас `container` :

```css
main {
  margin-top: 80px; /* або висота header */
}
```

Елемент:

- завжди залишається на екрані
- не рухається при прокрутці
- z-index: елемент завжди поверх інших елементів

## Крок 6. position: sticky

`sticky` — це поєднання relative та fixed.

Приклад: Додайте стилі до сторінки окремого продукту:

```css
.product {
  height: 1000px;
  position: relative;
}

.product-description {
  position: sticky;
  top: 50px;
  background: #ffffff;
  /* білий фон для опису */
}
```

Елемент:

- спочатку поводиться як звичайний
- а потім "прилипає" при прокрутці

## Крок 7. Для чого позиціонування в магазині

Позиціонування використовується для:

- фіксованого меню
- бейджів товарів
- кнопок дій
- інформаційних блоків

Без нього сучасний UI неможливий

💡 Після змін у проекті введіть в консолі:

```bash
git add .
git commit -m "lesson13"
git push  опиши цей крок для першого уроку

```

## Крок 8. Типові помилки

- використовувати absolute без потреби
- не розуміти, відносно чого позиціонується елемент
- зловживати fixed

## Підсумок уроку

На цьому уроці ви:

- розібрали всі типи position
- зрозуміли, де їх застосовувати
- підготувалися до складних макетів

🎯 Магазин стає ближчим до реального продукту.

У наступному уроці:
Flexbox — основи та практичні приклади
